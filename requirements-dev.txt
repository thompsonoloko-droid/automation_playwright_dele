# =====================================
# Development / CI-only Dependencies
# =====================================
# Install:  pip install -r requirements-dev.txt
# =====================================

# Pull in runtime dependencies first
-r requirements.txt

# Linting & Formatting (pre-commit runs ruff format + ruff lint via hooks)
ruff>=0.11.0                       # Fast Python linter & formatter
pre-commit>=4.0.0                  # Git hook framework for quality checks

# Static Type Checking
mypy==1.20.0                       # Static type checker
types-requests                     # Type stubs for requests
